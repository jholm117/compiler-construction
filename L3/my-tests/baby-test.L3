define :main () {
  v1 <- call print (5)
  v2 <- call :hello ()
  return v4
}

define :blue (p1, p2, p3) {
    call :myF (4, v1, v2, 6, 7, v3, v4, 5, v5, v6)
    call allocate (1, 2)
    return
}

define :red (p1) {
    call :blue (p1, 4, 5)
    return
}

define :myFun (p1, p2, p3, p4, p5, p6, p7, p8) {
  return p1
}
